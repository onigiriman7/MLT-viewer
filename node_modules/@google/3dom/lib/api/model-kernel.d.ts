import { ConstructedWithArguments, Constructor, Model, ThreeDOMElement, ThreeDOMElementMap } from '../api.js';
import { SerializedElementMap, SerializedModel, ThreeDOMMessageTypeMap } from '../protocol.js';
import { ImageConstructor } from './image.js';
import { MaterialConstructor } from './material.js';
import { ModelConstructor } from './model.js';
import { PBRMetallicRoughnessConstructor } from './pbr-metallic-roughness.js';
import { SamplerConstructor } from './sampler.js';
import { TextureInfoConstructor } from './texture-info.js';
import { TextureConstructor } from './texture.js';
import { ThreeDOMElementConstructor } from './three-dom-element.js';
export interface ModelKernelInterface {
    readonly model: Model;
    mutate(element: ThreeDOMElement, property: string, value: unknown): Promise<void>;
    deserialize<T extends keyof ThreeDOMElementMap>(type: T, serialized: SerializedElementMap[T]): ThreeDOMElementMap[T];
    getElementsByType<T extends keyof ThreeDOMElementMap>(type: T): Array<ThreeDOMElementMap[T]>;
    deactivate(): void;
}
export declare type ModelKernelConstructor = Constructor<ModelKernelInterface> & ConstructedWithArguments<[MessagePort, SerializedModel]>;
export declare type ModelKernel = InstanceType<ModelKernelConstructor>;
/**
 * A constructor factory for a ModelKernel class. The ModelKernel is defined
 * based on a provided implementation for all specified 3DOM scene graph
 * element types.
 *
 * The sole reason for using this factory pattern is to enable sound type
 * checking while also providing for the ability to stringify the factory so
 * that it can be part of a runtime-generated Worker script.
 */
export declare function defineModelKernel(ThreeDOMMessageType: ThreeDOMMessageTypeMap, ThreeDOMElement: ThreeDOMElementConstructor, Model: ModelConstructor, Material: MaterialConstructor, PBRMetallicRoughness: PBRMetallicRoughnessConstructor, Sampler: SamplerConstructor, Image: ImageConstructor, Texture: TextureConstructor, TextureInfo: TextureInfoConstructor): ModelKernelConstructor;
